import { FormEvent, useEffect, useState } from 'react';
import { api } from '../api/client';

type User={id:string;email:string;name:string;role:string;created_at:string};type Asset={id:string;name:string;description:string;price:number;owner_id:string|null;created_at:string};
export default function Admin(){const [users,setUsers]=useState<User[]>([]);const [assets,setAssets]=useState<Asset[]>([]);const [name,setName]=useState('');const [description,setDescription]=useState('');const [price,setPrice]=useState('');const [message,setMessage]=useState('');const [error,setError]=useState('');const load=async()=>{try{const [u,a]=await Promise.all([api<{users:User[]}>('/api/admin/users'),api<{assets:Asset[]}>('/api/admin/assets')]);setUsers(u.users);setAssets(a.assets)}catch(e){setError((e as Error).message)}};useEffect(()=>{void load()},[]);const create=async(e:FormEvent)=>{e.preventDefault();setMessage('');setError('');try{await api('/api/admin/assets',{method:'POST',body:JSON.stringify({name,description,price:Number(price)})});setName('');setDescription('');setPrice('');setMessage('Asset created');await load()}catch(e){setError((e as Error).message)}};return <><div className="hero"><div><p className="eyebrow">ADMIN</p><h1>Management console</h1><p>Create assets and inspect registered users.</p></div></div>{message&&<p className="success">{message}</p>}{error&&<p className="error">{error}</p>}<div className="admin-grid"><section className="card form"><h2>Add asset</h2><form onSubmit={create}><input placeholder="Asset name" value={name} onChange={e=>setName(e.target.value)} required/><textarea placeholder="Description" value={description} onChange={e=>setDescription(e.target.value)} required/><input type="number" min="0.01" step="0.01" placeholder="Price" value={price} onChange={e=>setPrice(e.target.value)} required/><button className="primary">Create asset</button></form></section><section className="card"><h2>Registered users ({users.length})</h2><div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td></tr>)}</tbody></table></div></section></div><section className="card"><h2>All assets ({assets.length})</h2><div className="table-wrap"><table><thead><tr><th>Name</th><th>Price</th><th>Owner</th></tr></thead><tbody>{assets.map(a=><tr key={a.id}><td>{a.name}</td><td>${a.price.toFixed(2)}</td><td>{a.owner_id||'Available'}</td></tr>)}</tbody></table></div></section></>}
